# ASCL.net Web Application Requirements
# Install with: pip install -r requirements.txt

# Core ASCL functionality (includes dm-dbcore and SQLAlchemy)
ascl_core>=0.1.0

# Web Framework
Flask>=3.0.0

# ASGI Server
uvicorn[standard]>=0.27.0  # Includes uvloop and httptools for better performance
asgiref>=3.7.0  # WSGI-to-ASGI adapter for running Flask with Uvicorn

# Database adapters - install the one(s) you need
# MySQL (currently used)
mysqlclient>=2.2.0  # MySQL adapter (recommended, C-based, fast)
# PyMySQL>=1.1.0  # Alternative: Pure Python MySQL adapter (uncomment if preferred)

# PostgreSQL (for future migration)
psycopg2-binary>=2.9.0  # PostgreSQL adapter

# Security
bcrypt>=4.0.0  # Secure password hashing

# Data handling
phpserialize>=1.3  # Parse PHP serialized data (legacy field format)

# Optional: Performance improvements
# cdecimal  # Obsolete: Built into Python 3 as 'decimal' module

# Optional: Error tracking
# sentry-sdk[flask]>=1.40.0  # Uncomment if using Sentry

# Development tools (optional)
# pytest>=7.4.0
# pytest-flask>=1.3.0
# black>=23.0.0
# flake8>=6.1.0
